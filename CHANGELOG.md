# Changelog

รูปแบบตาม [Keep a Changelog](https://keepachangelog.com/) และใช้ [Semantic Versioning](https://semver.org/)

> เลขเวอร์ชันอยู่ที่ `package.json` ที่เดียว แล้วถูกฉีดเข้าแอปตอน build
> ดูเวอร์ชันที่กำลังใช้งานได้จากป้าย `v…` มุมซ้ายบนของแอป (คลิกเพื่อดูรายละเอียด)

---

## [1.2.0] — 2026-08-08

### เพิ่ม
- **คลังสัญลักษณ์** — บันทึกสัญลักษณ์ที่เลือกไว้พร้อมหมวดงาน (ประตู/หน้าต่าง/ดวงโคม/เต้ารับ/สวิตช์/สุขภัณฑ์/สปริงเกลอร์/หัวจ่ายลม/เสา/กริด/ครุภัณฑ์) และรหัส นำกลับมาใช้ซ้ำได้ข้ามหน้าและข้ามโครงการ เก็บใน IndexedDB (schema v2)
- เก็บ **ขนาดจริงบนกระดาษ** ของสัญลักษณ์ จึงใช้กับแบบที่ render คนละความละเอียดได้
- **ปรับกรอบต้นแบบได้** — ลากมุมทั้งสี่เพื่อย่อ/ขยาย หรือลากทั้งกรอบเพื่อเลื่อน พร้อมแจ้งเตือนเมื่อผลลัพธ์เก่าไม่ตรงกับกรอบใหม่แล้ว
- **เลือกเกณฑ์ความคล้ายอัตโนมัติ** จากช่องว่างที่กว้างที่สุดในการกระจายของคะแนน แทนการให้ผู้ใช้จูนเอง
- รายการที่นับได้เข้า BOQ พร้อมรหัสและคำอธิบายจากหมวดงานโดยอัตโนมัติ
- หน้าต่าง "เกี่ยวกับโปรแกรม" แสดงเวอร์ชัน วันที่ build และประวัติการเปลี่ยนแปลง

### แก้ไข
- แก้บั๊กที่ทำให้ผลการค้นหาผิดทั้งหมด: สัญลักษณ์ต้นแบบเคยถูกย่อขนาดแยกจากภาพที่ใช้ค้นหา ทำให้พิกเซลไม่ตรงเฟสกัน ระบบจึงจับแม้แต่ตัวที่ผู้ใช้เลือกเองไม่ได้ ตอนนี้ตัดต้นแบบจากภาพเดียวกันโดยตรง
- เปลี่ยนวิธีให้คะแนนเป็น F₂ ที่เน้นความครบถ้วนมากกว่าความบริสุทธิ์ เพราะจุดที่หายไปต้องตามหาเอง ส่วนจุดที่เกินคลิกตัดออกได้

---

## [1.1.0] — 2026-08-08

### เพิ่ม
- **เครื่องมือนับสัญลักษณ์อัตโนมัติ** (`M`) — เลือกสัญลักษณ์หนึ่งตัวแล้วค้นหาตัวที่เหมือนกันทั้งแผ่น
- ประมวลผลใน Web Worker พร้อมแถบความคืบหน้าและปุ่มยกเลิก ไม่ค้างหน้าจอ
- ตรวจผลก่อนบันทึก คลิกวงบนแบบเพื่อตัดจุดที่ไม่ต้องการออกทีละจุด
- ปรับเกณฑ์ความคล้ายได้ทันทีโดยไม่ต้องค้นหาใหม่

---

## [1.0.0] — 2026-08-08

### เพิ่ม
- เครื่องมือวัด 2D: ความยาว เส้นต่อเนื่อง สี่เหลี่ยม พื้นที่ วงกลม มุม/ความลาด และนับจำนวน
- รองรับ PDF หลายหน้า และไฟล์ภาพ JPG/PNG/WEBP
- ปรับเทียบมาตราส่วนจากระยะที่ทราบค่า พร้อมมาตราส่วนสำเร็จรูป 1:1 ถึง 1:1000
- กริดตามระยะจริง สแนปจุด ล็อกมุม 45° และ undo/redo
- คำนวณปริมาณวัสดุ: คอนกรีต เหล็กเสริม ไม้แบบ งานก่อ งานตกแต่งผิว งานดิน
- ส่งออก BOQ เป็น Excel (3 ชีต) และ PDF พร้อมแนบภาพแบบที่มาร์กอัป
- เก็บประวัติการวัดใน IndexedDB พร้อมค้นหาและกรอง
- โหมด 3D สำหรับ OBJ/GLB/GLTF — วัดระยะและตัดหน้าตัด
- PWA ใช้งานออฟไลน์และติดตั้งเป็นแอปได้
